var submit = $("#edit_store_payment_method_<%= resource.id %> .cancel-edit-instruction").click();
if (typeof($editableField) == 'undefined') { $editableField = new EditableField( $('.editable-field') ); }
$editableField.cancelEditMode();