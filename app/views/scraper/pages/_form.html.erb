<%
	@scraper_page ||= ::Scraper::Page.new
-%>
<%= form_for(@scraper_page) do |f| %>
  <% if @scraper_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scraper_page.errors.count, "error") %> prohibited this scraper_page from being saved:</h2>

      <ul>
      <% @scraper_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
